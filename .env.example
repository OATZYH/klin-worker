# Klin-Worker Environment Configuration

# llama-server (out-of-process)
#   KLIN_LLAMA_SERVER_URL=http://127.0.0.1:8080/
#   KLIN_LLAMA_EMBEDDING_SERVER_URL=http://127.0.0.1:8081/
# Packaged sidecar flow uses the same default URL.
KLIN_LLAMA_SERVER_URL=http://127.0.0.1:8080/
KLIN_LLAMA_EMBEDDING_SERVER_URL=http://127.0.0.1:8081/
KLIN_EMBEDDING_DIM_SIZE=1024 # Qwen3-Embedding-0.6B default
KLIN_LLAMA_MAX_CONCURRENT_REQUESTS=1
KLIN_LLM_INPUT_MAX_CHARS=24000
KLIN_LLM_OUTPUT_MAX_TOKENS=4096
KLIN_RAG_CHUNK_TOKEN_SIZE=4096

# RAG ingest behavior
# KLIN_RAG_ENABLE_IMAGE_PROCESSING=true
# KLIN_RAG_ENABLE_TABLE_PROCESSING=false
# KLIN_RAG_ENABLE_EQUATION_PROCESSING=false
# KLIN_RAG_ENABLE_KG_EXTRACTION=false
# KLIN_RAG_CONTENT_FORMAT=auto
# KLIN_RAG_EMBEDDING_TIMEOUT_SECONDS=300

# Summary service tuning
# KLIN_SUMMARY_RETRIEVAL_TOP_K=2
# KLIN_SUMMARY_OUTPUT_MAX_TOKENS=2048

# Rename service tuning
# KLIN_RENAME_OUTPUT_MAX_TOKENS=48

# RAG service tuning
# KLIN_RAG_OUTPUT_MAX_TOKENS=512
# KLIN_SEARCH_SEMANTIC_MIN_SCORE=0.55

# App
KLIN_DEBUG=false

# Langfuse tracing
# KLIN_LANGFUSE_ENABLED=false
# KLIN_LANGFUSE_PUBLIC_KEY=pk-lf-...
# KLIN_LANGFUSE_SECRET_KEY=sk-lf-...
# KLIN_LANGFUSE_HOST=http://localhost:3000
# KLIN_LANGFUSE_DEBUG=false
# KLIN_LANGFUSE_SAMPLE_RATE=1.0
# KLIN_LANGFUSE_MAX_PAYLOAD_CHARS=4000

# Storage paths
# Dev default  → .storage/klin.db  and  .storage/rag_storage  (auto-detected)
# Prod default → value of KLIN_APP_DATA_DIR injected by Tauri (auto-detected)
# Uncomment below ONLY if you need to override the auto-detected paths:
# KLIN_DATABASE_PATH=.storage/klin.db
# KLIN_RAG_WORKING_DIR=.storage/rag_storage

# Semantic duplicate threshold (0.0 – 1.0)
# KLIN_SIMILARITY_THRESHOLD=0.85
